module.exports = {
  // Default GST rate if no HSN match is found.
  defaultRate: 0.18,

  // Extend with HSN-specific GST rates as needed.
  hsn: {
    // '8708': 0.28, // Example: vehicle parts
  },
};
